 #include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0),cin.tie(0);
	int l,r,x,y,k;
	bool can = false;
	cin >> l >> r >> x >> y >>k;
	for(int i=l;i<=r;i++){
			if((i%k==0 && (i/k>=x && i/k<=y)) can = true;
	}
	if(can){
		cout << "YES" << endl;
	}else{
		cout << "NO" << endl;
	}
	return 0;
}
