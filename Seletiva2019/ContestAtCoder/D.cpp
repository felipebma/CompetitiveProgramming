#include <bits/stdc++.h>

#define endl "\n"
using namespace std;

int main(){
	ios::sync_with_stdio(0);cin.tie(0);
	vector<int> arr;
	long long n;
	cin >> n;
	long long ans;
	ans = ((n-1)*n)/2;
	cout << ans;
	return 0;
}
