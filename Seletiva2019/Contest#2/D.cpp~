#include <bits/stdc++.h>

#define endl '\n'
using namespace std;

int main(){
	ios::sync_with_stdio(0);cin.tie(0);
	string s,t;
	cin >> s >> t;
	int i=0,j=0, cont;
	for(j=0;j<t.length() && i<s.length();j++){
		if(t[j]==s[i]){
			i++;
			cont++;
		}
	}
	cout << cont;
	return 0;
}
